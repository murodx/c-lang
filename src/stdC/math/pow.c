#include <stdio.h>
#include <math.h>

// The functions powf() and powl() are also defined
//
int main(void)
{
    printf("pow(2, 3) = %.f\n", pow(2, 3)); // 2 в степени 3
    printf("pow(5, 2) = %.f\n", pow(5, 2)); // 5 в степени 2
    return 0;
}
// the function pow return type double !
