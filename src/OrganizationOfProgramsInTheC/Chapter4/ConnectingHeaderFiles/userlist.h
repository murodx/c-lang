#include <stdio.h>
// подключаем заголовочный файл, где определена структура User
#include "user.h"  // already connect
// функция вывода массива структур User
void printUserList(struct User*, size_t);